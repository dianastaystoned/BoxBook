<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    <h3>Welcome {{user.fullname}}</h3>
                    <h4>{{user.email}}</h4>
                    <a href="/book/add" class="btn btn-primary m-4">
                        Let's started, add some books!  
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>